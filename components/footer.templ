package components

import (
	"strconv"
	"time"
)

templ Footer() {
	<footer class="bg-base-200 items-center p-4 border-t border-t-primary mt-20">
		<p class="text-center w-full">Copyright © { strconv.Itoa(time.Now().Year() ) } - All right reserved</p>
	</footer>
}
